
import styled from 'styled-components'
import { Tag } from '@/components/Tag'

const StyledTag = styled(Tag)`
  &.ant-tag {
    width: 15rem;
    border-radius: 4px;
  }
`

export {
  StyledTag as Tag,
}
