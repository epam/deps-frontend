
import styled from 'styled-components'
import { DualToggle } from '@/components/DualToggle'

const ViewSwitcher = styled(DualToggle)`
  && .ant-radio-button-wrapper {
    width: 8.2rem;
  }
`

export {
  ViewSwitcher,
}
