
import { mockSelector } from '@/mocks/mockSelector'

const highlightedFieldSelector = mockSelector(null)
const activePolygonsSelector = mockSelector([])

export {
  highlightedFieldSelector,
  activePolygonsSelector,
}
