apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    {{- include "deps-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deps-frontend.selectorLabels" . | nindent 4 }}
